---
import { getSessionCookieName } from '../../lib/admin-auth';

export const prerender = false;

Astro.cookies.delete(getSessionCookieName(), {
  path: '/',
});

return Astro.redirect('/admin/login');
---
