---
interface Props {
  href: string;
  label: string;
  icon?: string;
  variant?: 'primary' | 'secondary' | 'outline' | 'success' | 'dark' | 'whatsapp';
  target?: string;
  rel?: string;
  class?: string;
}

const {
  href,
  label,
  icon,
  variant = 'outline',
  target,
  rel,
  class: className = '',
} = Astro.props;

const variantClasses = {
  primary: 'btn-primary',
  secondary: 'btn-secondary',
  outline: 'btn-outline',
  success: 'btn-success',
  dark: 'btn-neutral',
  whatsapp: 'border-[#25D366] bg-[#25D366] text-neutral hover:bg-[#1DA851] hover:border-[#1DA851]',
};

import Icon from './Icon.tsx';
---

<a
  href={href}
  target={target}
  rel={rel}
  class:list={[
    'btn',
    variantClasses[variant],
    className,
  ]}
>
  {icon && <Icon name={icon} class="text-[18px]" />}
  <span>{label}</span>
</a>
