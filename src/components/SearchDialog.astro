---
---

<button
  type="button"
  class="js-search-open btn btn-ghost btn-circle btn-sm text-base-content/70 hover:bg-base-200 hover:text-base-content focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-base-100"
  aria-label="Cari di website"
  aria-haspopup="dialog"
>
  <svg aria-hidden="true" class="h-[22px] w-[22px]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="11" cy="11" r="7"></circle>
    <path d="m20 20-3.5-3.5"></path>
  </svg>
</button>

<div class="js-search-modal modal z-50 hidden bg-neutral/45 p-3 backdrop-blur-sm sm:p-4" role="presentation">
  <div
    role="dialog"
    aria-modal="true"
    aria-labelledby="search-dialog-title"
    class="modal-box flex max-h-[88vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl border border-base-300 bg-base-100 p-0 text-base-content shadow-[0_32px_80px_rgba(15,23,42,0.2)] sm:max-h-[80vh]"
  >
    <div class="border-b border-base-300/70 px-5 py-4">
      <div class="flex items-center gap-3">
        <svg aria-hidden="true" class="h-6 w-6 shrink-0 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="7"></circle>
          <path d="m20 20-3.5-3.5"></path>
        </svg>
        <h2 id="search-dialog-title" class="sr-only">Pencarian</h2>
        <input
          class="js-search-input input input-ghost min-w-0 flex-1 rounded-none border-0 bg-transparent px-0 text-base font-semibold text-base-content placeholder:text-base-content/45 focus-visible:ring-0"
          placeholder="Cari agenda, berita, layanan, atau dokumen"
          autocomplete="off"
        />
        <button
          type="button"
          class="js-search-close btn btn-ghost btn-circle btn-sm text-base-content/70 hover:bg-base-200 hover:text-base-content focus-visible:ring-2 focus-visible:ring-primary"
          aria-label="Tutup pencarian"
        >
          <svg aria-hidden="true" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 6 6 18"></path>
            <path d="m6 6 12 12"></path>
          </svg>
        </button>
      </div>
      <div class="js-search-filters mt-4 flex gap-2 overflow-x-auto pb-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"></div>
    </div>

    <div class="js-search-results min-h-0 flex-1 overflow-y-auto p-4">
      <div class="py-12 text-center">
        <div class="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-base-200 text-base-content/45">
          <span class="loading loading-spinner loading-lg text-primary"></span>
        </div>
        <p class="mt-3 text-[12px] font-semibold uppercase tracking-wider text-base-content/55">Memuat data...</p>
      </div>
    </div>
  </div>
</div>

<script>
  // @ts-nocheck
  // Search records are escaped before insertion; innerHTML is only used to render trusted UI templates efficiently.
  const typeOrder = ['semua', 'berita', 'agenda', 'layanan', 'data', 'galeri'];
  const typeMeta = {
    berita: { label: 'Berita', icon: 'news' },
    agenda: { label: 'Agenda', icon: 'calendar' },
    layanan: { label: 'Layanan', icon: 'file' },
    data: { label: 'Data', icon: 'chart' },
    galeri: { label: 'Galeri', icon: 'image' },
  };

  const root = document.currentScript?.parentElement;
  const openButton = root?.querySelector('.js-search-open');
  const modal = root?.querySelector('.js-search-modal');
  const closeButton = root?.querySelector('.js-search-close');
  const input = root?.querySelector('.js-search-input');
  const filters = root?.querySelector('.js-search-filters');
  const resultsEl = root?.querySelector('.js-search-results');

  let records = [];
  let loaded = false;
  let loading = false;
  let activeType = 'semua';
  let previousOverflow = '';

  const normalize = (value) => String(value || '')
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9\s-]/g, ' ')
    .replace(/\s+/g, ' ')
    .trim();

  const haystack = (item) => normalize([
    item.title,
    item.description,
    item.type,
    item.category,
    ...(item.keywords || []),
  ].filter(Boolean).join(' '));

  const iconSvg = (name, className = 'h-5 w-5') => {
    if (name === 'chevron') return `<svg aria-hidden="true" class="${className}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"></path></svg>`;
    if (name === 'empty') return `<svg aria-hidden="true" class="${className}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"></circle><path d="m20 20-3.5-3.5"></path><path d="m7 7 8 8"></path></svg>`;
    return `<svg aria-hidden="true" class="${className}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"></circle><path d="m20 20-3.5-3.5"></path></svg>`;
  };

  const escapeHtml = (value) => String(value || '').replace(/[&<>'"]/g, (char) => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
  })[char]);

  const formatDate = (value) => {
    if (!value) return '';
    try {
      return new Intl.DateTimeFormat('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }).format(new Date(value));
    } catch {
      return '';
    }
  };

  const getCounts = () => typeOrder.reduce((acc, type) => {
    acc[type] = type === 'semua' ? records.length : records.filter((item) => item.type === type).length;
    return acc;
  }, {});

  const renderFilters = () => {
    if (!filters) return;
    const counts = getCounts();
    filters.innerHTML = typeOrder.map((type) => {
      const meta = type === 'semua' ? { label: 'Semua' } : (typeMeta[type] || { label: type });
      const active = activeType === type;
      return `<button type="button" data-search-type="${type}" class="btn btn-sm rounded-full ${active ? 'btn-primary' : 'btn-outline'}">${type === 'semua' ? iconSvg('travel', 'h-4 w-4') : iconSvg(meta.icon, 'h-4 w-4')} ${escapeHtml(meta.label)} <span class="badge badge-sm ${active ? 'badge-primary-content border-primary-content/40 bg-primary-content/15 text-primary-content' : 'badge-ghost'}">${counts[type] ?? 0}</span></button>`;
    }).join('');
  };

  const sortRecords = (items) => [...items].sort((a, b) => {
    const priority = (item) => item.priority === 'high' ? 1 : 0;
    const date = (item) => item.date ? new Date(item.date).getTime() : 0;
    return priority(b) - priority(a) || date(b) - date(a);
  });

  const renderResults = () => {
    if (!resultsEl || !input) return;
    if (loading) return;

    const query = normalize(input.value);
    const quick = sortRecords(records).slice(0, 5);
    const filteredByQuery = query ? records.filter((item) => haystack(item).includes(query)) : quick;
    const results = sortRecords(filteredByQuery)
      .filter((item) => activeType === 'semua' || item.type === activeType)
      .slice(0, 10);

    if (results.length === 0) {
      resultsEl.innerHTML = `<div class="py-16 text-center"><div class="mx-auto grid h-16 w-16 place-items-center rounded-full bg-base-200 text-base-content/45">${iconSvg('empty', 'h-8 w-8')}</div><p class="mt-4 text-base font-bold text-base-content">Tidak ditemukan</p><p class="mt-1 text-sm text-base-content/70">Coba kata kunci lain atau ubah tipe pencarian.</p></div>`;
      return;
    }

    const intro = query ? '' : `<div class="card mb-4 border border-base-300/70 bg-base-200/60 shadow-none"><div class="card-body p-4"><p class="text-[12px] font-semibold uppercase tracking-wider text-primary">Akses cepat warga</p><p class="mt-1 text-sm text-base-content/70">Mulai dari agenda penting, informasi terbaru, atau layanan yang paling sering dicari.</p></div></div>`;

    resultsEl.innerHTML = `${intro}<div class="space-y-1"><p class="mb-2 px-2 text-[12px] font-semibold uppercase tracking-wider text-base-content/55">${query ? `${results.length} hasil ditemukan` : 'Saran cepat'}</p>${results.map((item) => {
      const meta = typeMeta[item.type] || { label: item.type, icon: 'search' };
      const date = formatDate(item.date);
      return `<a href="${escapeHtml(item.url)}" class="group flex items-start gap-4 rounded-2xl p-3 transition-colors hover:bg-base-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"><div class="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-primary/10 text-primary transition-colors group-hover:bg-primary group-hover:text-primary-content">${iconSvg(meta.icon, 'h-5 w-5')}</div><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2 text-[11px] font-semibold uppercase tracking-wider text-base-content/55"><span>${escapeHtml(meta.label)}</span>${item.priority === 'high' ? '<span class="badge badge-error badge-sm">Penting</span>' : ''}${date ? `<span class="normal-case tracking-normal">${escapeHtml(date)}</span>` : ''}</div><h3 class="truncate text-sm font-bold leading-tight text-base-content transition-colors group-hover:text-primary">${escapeHtml(item.title)}</h3><p class="mt-0.5 line-clamp-2 text-[12px] text-base-content/70">${escapeHtml(item.description)}</p></div>${iconSvg('chevron', 'h-[18px] w-[18px] text-base-content/45 transition-transform group-hover:translate-x-0.5 group-hover:text-primary')}</a>`;
    }).join('')}</div>`;
  };

  const loadSearch = async () => {
    if (loaded || loading) return;
    loading = true;
    try {
      const response = await fetch('/api/search.json');
      records = await response.json();
    } catch {
      records = [];
    } finally {
      loaded = true;
      loading = false;
      renderFilters();
      renderResults();
    }
  };

  const openSearch = () => {
    if (!modal || !input) return;
    previousOverflow = document.body.style.overflow;
    document.body.style.overflow = 'hidden';
    modal.classList.remove('hidden');
    input.focus();
    loadSearch();
  };

  const closeSearch = () => {
    if (!modal || !input) return;
    document.body.style.overflow = previousOverflow;
    modal.classList.add('hidden');
    input.value = '';
    activeType = 'semua';
    renderFilters();
  };

  openButton?.addEventListener('click', openSearch);
  closeButton?.addEventListener('click', closeSearch);
  input?.addEventListener('input', renderResults);
  filters?.addEventListener('click', (event) => {
    const button = event.target instanceof Element ? event.target.closest('[data-search-type]') : null;
    if (!button) return;
    activeType = button.getAttribute('data-search-type') || 'semua';
    renderFilters();
    renderResults();
  });
  modal?.addEventListener('mousedown', (event) => {
    if (event.target === modal) closeSearch();
  });
  window.addEventListener('keydown', (event) => {
    if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === 'k') {
      event.preventDefault();
      openSearch();
    }
    if (event.key === 'Escape' && modal && !modal.classList.contains('hidden')) {
      closeSearch();
    }
  });

  renderFilters();
</script>
